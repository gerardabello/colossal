image: node

pages:
  stage: deploy
  script:
  - npm update
  - npm run build
  - mkdir .public
  - mkdir .public/build
  - mv build .public/build
  - cp index.html .public
  - cp .public public
  artifacts:
    paths:
    - public
  only:
  - master
